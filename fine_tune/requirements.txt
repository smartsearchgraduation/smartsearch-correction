# Fine-tuning Requirements for E-Commerce Typo Correction
# Install with: pip install -r requirements.txt

# ============================================
# CORE DEPENDENCIES
# ============================================
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0  # LoRA/QLoRA için
bitsandbytes>=0.41.0  # Quantization için
trl>=0.7.0  # SFT Trainer

# ============================================
# MODEL SPECIFIC
# ============================================
# ByT5
sentencepiece>=0.1.99

# ONNX Export (production inference)
onnx>=1.15.0
onnxruntime>=1.16.0
optimum>=1.14.0

# ============================================
# OLLAMA INTEGRATION (Qwen/Llama)
# ============================================
# ollama CLI kurulumu ayrıca gerekli: https://ollama.ai
# pip install ollama  # Python client (opsiyonel)

# ============================================
# DATA PROCESSING
# ============================================
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# ============================================
# EVALUATION
# ============================================
evaluate>=0.4.0
nltk>=3.8.0
Levenshtein>=0.23.0  # Edit distance hesaplama

# ============================================
# UNSLOTH (OPSİYONEL - 2x hızlı fine-tune)
# ============================================
# GPU varsa Unsloth kullan (çok daha hızlı):
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# ============================================
# NOTES
# ============================================
# 1. CUDA 11.8+ önerilir
# 2. Minimum 4GB VRAM (ByT5), 6-8GB (Qwen/Llama)
# 3. CPU-only için bitsandbytes'ı kaldırın
# 4. Mac M1/M2 için: pip install torch torchvision torchaudio
